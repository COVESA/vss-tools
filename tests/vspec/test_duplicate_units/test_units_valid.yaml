# Test file with valid (non-duplicate) unit descriptions
# This should work without errors

g/s:
  definition: Mass per time measured in grams per second
  unit: grams per second
  quantity: mass-per-time
  allowed-datatypes: ['numeric']

kg/s:
  definition: Mass per time measured in kilograms per second
  unit: kilograms per second  # FIXED - unique description
  quantity: mass-per-time
  allowed-datatypes: ['numeric']
